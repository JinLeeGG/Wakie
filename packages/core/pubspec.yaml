name: wakieai_core
description: WakieAI shared engine — account model, provider adapters, status scraping.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
